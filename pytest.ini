[pytest]
# Only look for tests in the `tests` folder to avoid collecting UI/dev helper files in `src`
testpaths = tests

# Make the project root importable so tests can `import src.*` if needed
pythonpath = .

# Avoid searching virtualenv/.git and scanner work directories
norecursedirs = .venv .git .scannerwork
